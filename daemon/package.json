{
  "name": "@mastra-cc/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "scripts": {
    "build": "tsdown && mkdir -p dist/tools/pins && cp ../tools/pins/deny-list.json dist/tools/pins/deny-list.json",
    "lint": "oxlint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "oxlint": "^1.77.0",
    "tsdown": "^0.22.14",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@mastra-cc/protocol-types": "workspace:*",
    "dbus-native": "0.15.1"
  }
}
