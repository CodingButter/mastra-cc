# Architecture Decision Records

Read them before writing code. They are the reason the layout in [01-ARCHITECTURE.md](../01-ARCHITECTURE.md) looks the way it does.

Two kinds of record live here, and the difference matters when you are deciding how much to trust one:

- **0001–0016 are back-filled from evidence** in the `computer-controls` prototype: 332 commits, 127 pull requests, and 105 issues over seven days (2026-08-01 → 2026-08-08). None of them are speculative. Each names the commit, PR, issue, or measurement that produced it.
- **0027–0032 are findings-forced**, written 2026-08-09 at the close of M0.5. Each names the measurement that forced it and links the artifact in [docs/proofs/](../proofs/). Six earlier records were superseded by them — 0020 entirely, and 0008, 0010, 0019, 0021 and 0023 in part. None were edited: each carries a header note saying which part died and why, because the reason a decision was made is as useful as the decision, especially when the premise turns out to be wrong.
- **0017–0026 are forward decisions**, taken 2026-08-08 while scoping the rebuild. They are argued from prototype evidence and from source read in a live checkout, but several rest on beliefs that have not been probed yet. Where that is true the record says so, and the belief is filed as a question in [09-QUESTIONS.md](../09-QUESTIONS.md). A forward decision that a spike contradicts gets superseded, not quietly edited.

## The records

| # | Decision | One-line reason |
|---|---|---|
| [0001](0001-machine-config-lives-in-the-repo.md) | Machine configuration lives in the repository | Three outages came from config no test could see |
| [0002](0002-schema-freeze-is-a-ci-job.md) | The protocol freeze is a CI job, not a comment | The prototype's freeze was prose; the file changed 23 times |
| [0003](0003-one-shared-transport-package.md) | One shared transport package, from commit one | A second, drifted daemon client had skipped the digest check |
| [0004](0004-semantic-first-pixels-last.md) | Semantic first; pixels last, addressed by window | The founding bet; it held for seven days across four toolkits |
| [0005](0005-wake-is-enrolment-first-fingerprinting.md) | Wake is enrolment-first fingerprinting *(admission superseded by 0053)* | The recogniser heard "hey mastra" as "He master." |
| [0006](0006-hub-holds-no-audio.md) | The hub holds no realtime conversation audio *(absolute form narrowed by 0053)* | Privacy, latency, and topology all point the same way |
| [0007](0007-identity-is-derived-credentials-are-minted.md) | Identity is derived; credentials are minted | "Never give the key to the agent or they'll try it on every door" |
| [0008](0008-scopes-operation-classes-and-honest-refusals.md) | Scopes, operation classes, honest refusals | A refusal blamed a flag that was demonstrably present |
| [0009](0009-generated-code-is-build-output.md) | Generated code is build output, not source | Three generated files churned in lockstep with the schema |
| [0010](0010-daemon-is-python-single-threaded-default-glib-context.md) | Daemon: Python, one thread, default GLib context | The wrong main context loses events silently |
| [0011](0011-dashboard-is-vite-with-playground-ui.md) | Dashboard is Vite + playground UI from commit one | The migration became a gate on unrelated work |
| [0012](0012-claims-needing-a-desktop-are-proved-by-artifact.md) | Desktop claims are proved by artifact | A proof that cannot prove must write nothing |
| [0013](0013-episodes-are-a-git-graph.md) | Episodes are a git graph | Inspectable with tools people already have |
| [0014](0014-monorepo-droppable-from-commit-one.md) | Monorepo-droppable from commit one | The day-seven reshape cost 179 files and one outage |
| [0015](0015-one-vertical-slice-before-parallel-agents.md) | One vertical slice before parallel agents | Rework was the constraint, never throughput |
| [0016](0016-the-face-is-a-managed-window-that-hides-when-told.md) | The face is a managed window that hides when told | Unfocusable on X11 means unmanaged, and "on top" silently stops working |
| [0017](0017-platform-backends-live-inside-the-daemon.md) | Platform backends live inside the daemon | Hub and clients must not learn which operating system they are on |
| [0018](0018-the-protocol-speaks-a-neutral-element-vocabulary.md) | The protocol speaks a neutral element vocabulary | A platform's words in the schema become a version bump later |
| [0019](0019-capability-is-not-authority.md) | Capability is not authority | The operating system's permission is a precondition, never consent |
| [0020](0020-granting-an-application-is-a-transaction-with-a-rollback.md) | Granting an application is a transaction with a rollback | Runtime edits to launcher entries are facts no test in the tree can see |
| [0021](0021-standing-authority-is-armable-attestation-is-not-waivable.md) | Standing authority is armable; attestation is not waivable | Prompting for every write is how assistants become babysitting |
| [0022](0022-failure-to-act-is-harm-we-caused.md) | Failure to act is harm we caused | Every protection must fail toward informing, not toward stopping quietly |
| [0023](0023-the-phone-is-a-consent-surface.md) | The phone is a consent surface | The decision travels with the person; the authority never leaves the hub |
| [0024](0024-a-task-in-flight-can-be-steered.md) | A task in flight can be steered | Stop and start are not the only two things a person may want to say |
| [0025](0025-the-agent-fleet-only-fixes-defined-behaviour.md) | The agent fleet only fixes defined behaviour | If there is no intended behaviour yet, the fleet does not get to invent it |
| [0026](0026-the-audit-log-is-an-access-record-episodes-are-the-narrative.md) | The audit log is an access record; episodes are the narrative | A receipt you can rewrite is not a receipt |
| [0027](0027-the-assistant-opens-the-application-itself.md) | The assistant opens the application itself | Readability is decided at launch, so there is nothing on your system to rewrite |
| [0028](0028-trust-is-a-mode-and-the-default-asks-almost-nothing.md) | Trust is a mode, and the default asks almost nothing | People want to hand over a task and leave; the record survives every mode |
| [0029](0029-the-daemon-knows-what-it-launched.md) | The daemon knows what it launched | It can only ever disclaim its own, never claim yours |
| [0030](0030-the-daemon-is-one-node-process.md) | The daemon is one Node process; Linux does not need Python | Accessibility is plain D-Bus underneath; the binding was a convenience, not a gate |
| [0031](0031-the-agent-emits-a-plan-a-model-free-interpreter-runs-it.md) | The agent emits a plan; a model-free interpreter runs it | A transcript cannot be reviewed, replayed, or measured |
| [0032](0032-the-page-layer-is-an-instrument-not-a-gate.md) | The page layer is an instrument, not a gate | Five of eight paths observed — a gate with holes is worse than none |
| [0033](0033-the-schema-arrives-at-one-point-oh.md) | The schema arrives at 1.0.0, and its introduction goes through its own gate | A gate whose first act is an exemption has taught everyone how to ask for the second one |
| [0034](0034-launch-is-the-first-effect-class-operation.md) | Launch is the first effect-class operation, and B11 arrives with it | Authority before capability, one byte-identical refusal, and the timing pin lands in the same commit as the operation it pins |
| [0035](0035-the-browser-is-read-through-its-own-protocol.md) | The browser is read through its own protocol, over a hand-rolled channel | One recordable exchange seam over the debugging endpoint - discovery included - so the offline lane replays a real browser; Playwright rejected for hiding the wire |
| [0036](0036-grants-live-in-a-file-the-daemon-owns.md) | Grants live in a file the daemon owns | Deny-by-default observe visibility from a daemon-local permissions file ∪ session flags ∪ launch permits, enforced inside the walk so an ungranted subtree is never read; the one permitted read is the name |
| [0037](0037-the-other-three-classes-are-on-the-wire-before-they-are-possible.md) | The other three classes are on the wire before they are possible | schema version 1.2.0 defines editElement/activateElement/submitElement as refused-by-name methods that never touch a backend; attestation required in the contract from day one; classes live in the B11-pinned dispatch table, not the schema |
| [0038](0038-a-browser-profile-is-a-launch-identity.md) | A browser profile is a launch identity | Named browser profiles are catalog keys composed at boot from an operator file, not a schema parameter; observe names expand through the recipe's appears-as tree name while launch authority never does; the daemon never looks inside a profile directory |
| [0039](0039-the-desktop-talks-first.md) | The desktop talks first | schema version 1.3.0 adds subtree-scoped subscriptions and a server-initiated change event; priority is carried and never interpreted; an event is a pointer with no content field; attribution is exactly self, external and unattributed, produced mechanically from the one serialised call in flight and never read off the screen |
| [0040](0040-a-visibility-verdict-carries-its-route.md) | A visibility verdict carries its route | Every element answer stamps `diagnostic["mastra-cc/visibility-route"]` naming the instrument that produced it — provenance, not vocabulary, so the schema stays unchanged; the label says which instrument answered, never that it is right, because bounds alone is a liar (M0.5: browser route 10/10, platform route 6/10) |
| [0041](0041-agents-live-in-the-hub-clients-are-thin.md) | Agents live in the hub; clients are thin faces | The orb and the orchestrator are both hub-resident with their own threads and memory; a client carries a microphone, a speaker, pixels and a socket. Moving the conversational agent to a client buys a fraction of a percent of latency and pays for it with the provider key on every device (B3). The hub reaches the daemon through `packages/transport` only, and a capability it lacks lands inside it — the pressure that drifted the prototype's second client arrives in M3 |
| [0042](0042-existence-is-readable-content-is-not.md) | Existence is readable; content is not | Supersedes ADR-0008 rule 6: an unpermitted application is listed with its refused capabilities and the setting behind them, instead of being hidden. Invisibility produced a *false belief* — an agent that cannot see OBS tells the user to install what they already have. Everything inside an application stays behind the grant; the trade given up is that a compromised agent now learns the installed list |
| [0043](0043-an-element-publishes-its-own-actions.md) | An element publishes its own actions | The role→action tables in both backends were invented, never measured — nothing ever asked the platform. Actions are read from the element, the closed four-value enum becomes an open vocabulary, and every action reports one of three states (available / disabled-by-configuration / not-exposed) that are never collapsed. Scroll was never a design question. The user configures; the daemon enforces; the capability set is finished before the hub is built |
| [0044](0044-the-assistant-does-not-take-the-desk.md) | The assistant does not take the desk | Focus moving as a *side effect* is a bug; focus moving because the user asked is `activate`. Launch records focus and restores it, as a tested behaviour, and says so when it cannot. The daemon does nothing about this today — and whether restoration works on Wayland is a measurement the implementing milestone must take, not a claim this record makes |
| [0045](0045-actions-are-verbs-operations-carry-magnitudes.md) | Actions are verbs; operations carry the magnitudes | `DoAction` takes an index and nothing else on all three target platforms, so a parameterised action is structurally inexpressible — measured, not assumed. Actions stay open, parameterless and unnormalised; a small designed set of neutral operations carries the typed arguments; magnitudes are read from the range the element publishes, so percent is a reading rather than a unit we impose. Scroll is a reveal operation, correcting ADR-0043's claim that it arrives free |
| [0046](0046-raw-input-is-the-most-restricted-class-not-a-banned-one.md) | Raw input is the most restricted class, not a banned one | Amends ADR-0004: the ban becomes an operation class that is off by default, never self-granted, never a fallback from a semantic failure, always attributed as its own class, and enabled only by the user. A capability that cannot exist under any configuration produces the same false belief ADR-0042 removed one level up. The cost is named — the ban's deterrent was that the shortcut required deleting a test, and containment is weaker than absence |
| [0047](0047-the-wire-carries-words-we-did-not-invent.md) | The wire carries words we did not invent | Schema version 1.4.0. Action names open because the shipped four-word enum shared *zero* words with the live desktop; the availability verdict beside them stays closed, with three states that must never collapse. Amends ADR-0018 clause 2 in one respect only — what the daemon decides stays closed, what applications publish opens. Adds the four ADR-0045 operations with published ranges, scroll as reveal, and the application-listing method ADR-0042 needed to exist in. The cost is named — an open name is a name nobody reviewed, and the compiler stops helping where it used to |
| [0048](0048-the-words-gmails-inbox-publishes-carried-as-words.md) | The words Gmail's inbox publishes, carried as words | Schema version 1.5.0. Promotes grid, row and gridcell to neutral roles - the words the measured inbox actually published (154 gridcells, 100 rows, 1 grid on the signed-in profile), passing ADR-0047's test because the desktop offered them. Maps Chromium's checkbox spelling to the existing role. Landmarks and tab stay in the diagnostic, a recorded state not a loss. The bump also discharges Segment 1's prose debt: seven method descriptions that still claimed refusal-by-constant. The cost is named - a wider vocabulary is more surface for a backend to mis-map, and the golden, digest and transport all move with it |
| [0049](0049-what-a-launched-applications-death-is-tied-to.md) | What a launched application's death is tied to | Issue #14's wrapper-fork premise was measured false: google-chrome execs in place and a SIGTERMed daemon reaps every recipe's tree. The real orphan mechanism was an unhandled SIGHUP from a closing shell - the fix adds SIGHUP to the signals that run terminateOwned, a new trigger over the same table-owned targets, so ADR-0027/0029's never-signal-a-foreign-process guarantee is untouched. SIGKILL and crash paths remain a named limitation: cleanup that survives them needs a persisted ownership table, which does not yet exist |
| [0050](0050-the-record-names-the-refusal-not-the-sentence.md) | The record names the refusal, not the sentence | An access record whose `outcome` stored the refusal the caller was handed would leak user text: this daemon's refusal sentences quote element names and observed values (backend.ts:185, atspi/effects.ts:212, atspi/index.ts:507, focusNotRestored), which ADR-0042 says is not readable. So `outcome` carries a refusal CLASS from a closed enumerated set asserted by test, never the sentence, and the narrowing of an element to id-and-role happens once inside the audit module rather than at each call site. Amends ADR-0026 three ways: the told-anyone field is not carried (the daemon knows one effect at a time and cannot answer honestly), the record names what a query ANSWERED rather than what it walked, and the seven-key set is frozen so a new key is a new ADR. No encryption at rest, no retention limit and no rotation are shipped, which matters more than for an effect-only log because reads write entries too - growth tracks the query rate |
| [0051](0051-what-always-on-top-is-worth-under-a-focused-full-screen-window.md) | What always-on-top is worth under a focused full-screen window | ADR-0016's evidence table and the M4 exit box both say the face survives a full-screen window, unconditionally. Measured on the repository's own two-output X11 desk, that is false in one condition and true in the other: a window manager promotes a full-screen window that HOLDS FOCUS into a layer above the ABOVE layer, so the face is buried while that window has focus and returns to the top by itself the moment focus leaves. `_NET_WM_WINDOW_TYPE_DOCK` does not change it. The window model is not amended - a managed window yielding to a focused full-screen window is correct behaviour, and the only way to override it is override-redirect, which is the prototype's first wrong model that ADR-0016 exists to record. What changes is the sentence: the guarantee is stated with its condition, the exit box is ticked with the condition in a footnote rather than reworded, and the harness scores the box as `measured` and refuses to render an unconditional claim. The cost recorded: a buried face gives the user no indication the assistant is trying to reach them, and this was measured under one window manager |
| [0052](0052-the-lane-carrier-is-transports-second-wire.md) | The lane carrier is transport's second wire, not the hub's first socket | Two written constraints pulled opposite ways: `lanes.ts:14-18` says the M4 carrier lands inside `packages/transport` and never beside it, because a WebSocket server in the hub would leave pin B5 green while breaching the boundary B5 defends; ADR-0003 says its four-responsibility list is normative and a fifth requires an ADR, and those four describe the DAEMON wire while a hub-client lane is a different wire with a different peer. The list bends: transport owns a fifth thing, the hub-client lane wire, Node-builtins-only over a unix socket with the same newline-delimited framing. The lane wire deliberately has no digest handshake because the vocabulary is not generated - its equivalent guarantee is that a frame naming an event outside the frozen four is rejected where it arrives. Costs recorded: the package is one step nearer the junk drawer its own risk clause names, B5's guarantee is now half pin and half a source test in the widget that protects only that client, and a reader must know which wire an export belongs to |
| [0053](0053-phrase-wake-gates-a-client-owned-voice-session.md) | Phrase wake gates a client-owned voice session | Speaker-specific enrolment answered the wrong question and failed to generalize reliably in live use. The client detects only the phrase, buffers one bounded opening, and sends it directly to one constrained Gemini Live session. Exactly two initial controls admit or stop the conversation; malformed, conflicting, absent, timed-out, or failed decisions close silently. The hub holds no audio and mints only the dial. Follow-ups stay active until dismissal, provider failure, or 60 seconds without user speech. The realtime model has no desktop execution authority. Chromium owns echo-cancelled capture and playback |
| [0054](0054-gmail-authority-is-composed-by-the-operator-unit.md) | Gmail authority is composed by the operator-owned unit | M6 needs one reproducible authority tuple without handing a widening method to the model. The unit owns the boot join: launch exactly `gmail`, observe `gmail` plus its `chrome` tree identity, durable launch enabled only for Gmail, and an explicit protected audit destination. Operator files are seeded once and then preserved; profiles remain absent; installation does not enable the unit or launch software |
| [0055](0055-orchestration-requests-launch-the-daemon-decides-it.md) | Orchestration requests launch; the daemon decides it | Trusted hub orchestration may choose when to request one identity, but it owns no permit list and receives no authority answer except the daemon's unchanged result. The model receives no launch tool. Stage 4 may put a typed lifecycle around this seam but may not turn its identity into arbitrary model-chosen arguments |
| [0056](0056-permitted-content-is-observable-protected-content-is-redacted.md) | Permitted content is observable; protected content is redacted | Application grants admit content observation, but platform-marked protected controls carry only a structured redaction; audit records and change events remain content-free |
| [0057](0057-mastra-cc-is-a-peripheral-not-an-assistant.md) | mastra-cc is a peripheral, not an assistant | The product is the daemon plus an installable package that knows how to drive it. The client surface — hub, widget, dashboard, voice, and the lane wire that fed them — is removed rather than maintained for a reader who no longer exists. A talking assistant becomes a later composition over the same runtime |
| [0058](0058-the-daemon-serves-one-protocol-through-two-front-doors.md) | The daemon serves one protocol through two front doors | An opt-in WebSocket listener beside the unix socket, driven by the same per-connection handler and carrying the same bytes. Off unless `--ws-port` asks for it, loopback unless `--ws-host` widens it, and authentication left deliberately to whoever composes a product on top |
| [0059](0059-the-package-ships-primitives-and-instructions-not-macros.md) | The package ships primitives and instructions, not macros | `@mastra-cc/desktop` wraps the one transport client, exposes the protocol one for one, and ships the agent instructions inside the tarball with a byte-identity test against the repository's copy. Mastra tools are a subpath export with `@mastra/core` as a peer, their descriptions and schemas read from the generated descriptors, and refusals passed back verbatim |
| [0060](0060-an-agent-id-selects-a-connection-because-the-cause-id-comes-back-on-the-event.md) | An agent id selects a connection, because the cause id comes back on the event and not on the call | The package is an instance addressed once, and both `getTools(id)` and `getSignalProvider(id)` resolve the same agent id to the same dial. Attribution stays true by construction rather than by inference, at the cost of one socket per agent. Carrying the id on the wire is the better end state and is deferred, because it moves `SCHEMA_DIGEST` |

## Writing a new one

Use the next free number. Keep the four sections: **Context**, **Decision**, **Consequences**, **Evidence**.

Two rules, both of which the documents above try to honour:

1. **Every claim carries a receipt.** A commit hash, a PR or issue number, a `file:line`, or a measurement with the command that produced it. A claim without one is a belief, and beliefs are what this whole exercise is trying to replace.
2. **State the cost.** A decision with no listed downside has not been thought about. If the Consequences section only contains good news, the record is not finished.

Superseding an ADR means writing a new one that says so in its header and updating the old one's status. Do not edit a decision's history — the reason it was made is as useful as the decision, especially when it turns out to be wrong.
