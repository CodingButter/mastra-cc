# CC-01–CC-09 remaining-requirements audit

Audit baseline: `fix/reliability-remediation` at `3ae6a36`; Mousepad evidence lives separately on `feat/mousepad-verified-find-replace` at `38c5884`. Source plan: `/tmp/mastra-cc-remediation-plan.md`, copied from Bigbeast. This is an acceptance-gap audit, not a claim that every historical test receipt was rerun or that these branches are integrated.

The corresponding `cc01` through `cc09` proof directories retain the implemented slices and test receipts. Passing a slice does not close the entire numbered requirement. Below, “open” includes evidence that has not been established, rather than asserting the implementation necessarily fails.

| Item / plan lines | Established bounded slice | Unaddressed or unproven acceptance requirements |
| --- | --- | --- |
| CC-01 / 55–83 | Corrected visible-pixel contract and generated-description/pixel fixture checks (`cc01/README.md`) | Live authorized foreground preparation, failure/focus-restoration interaction, covered/changed-layout characterization. Capture must remain observational; foreground does not establish non-occlusion. |
| CC-02 / 85–135 | Actual native scripted-channel Unicode/selection/delay tests and server evidence preservation (`cc02/README.md`) | Live IME/autocomplete/selection transformations; post-emission exception and human-takeover-safe focus restoration; adapter-level uncertain-attempt/no-duplicate retry proof. Preserve explicit UTF-16 input-limit decision, malformed-surrogate policy and semantic/raw separation. |
| CC-03 / 137–157 | Controlled startup/shutdown generation races and terminal-stop tests (`cc03/README.md`) | No claim of retracting or awaiting already-submitted notifications. Framework delivery-settlement ownership is a separate integration boundary, not provider-local cancellation. |
| CC-04 / 159–179 | Hard pointer bounds, expiry, aggregate rate limit, trailing dirty delivery and nondefault gaps (`cc04/README.md`) | Immediate watch-end/unsubscribe cleanup; no extra queued wake after explicit removal; real framework final-observation semantics. Expiration is not immediate lifecycle integration. |
| CC-05 / 181–215 | Real Unix/WebSocket contender exclusion and queued/disconnect authority tests (`cc05/README.md`) | Complete-task serialization on a shared connection; explicit human revoke/resume; checkpoints before additional native emissions and restoration; uncertain outcome/re-observation after takeover; demo uses shared control state. Global OS exclusivity is not enforced; retain one-authority-per-desktop deployment rule. |
| CC-06 / 217–241 | Honest unknown-origin events and independent server/subscriber tests (`cc06/README.md`) | Active-task staleness/completion accounting for unknown events; safe replacement idle-wake policy with a no-self-triggering-loop test; transfer/takeover integration and visibility revocation throughout that task lifecycle. Raw event availability alone is not task-state accounting. |
| CC-07 / 243–274 | Explicit conservative refusal of partial images, each-edge pixel proof (`cc07/README.md`) | Stronger additive crop provenance and model-output preservation; normalized center/edge mapping, finite/bounds validation, display-origin/scaling/rounding tests; capture identity and layout/content freshness checks. These are deferred by the plan's permitted interim refusal, not secretly implemented. |
| CC-08 / 276–300 | Fresh bounded membership, unknown recovery, cycle/depth/close seam tests (`cc08/README.md`) | Real native reparenting/root-removal proof; explicit root-level degraded-watch health without unauthorized descendant disclosure; measured ancestry budget and pending-work/shutdown bounds; full server subscription-establishment buffering proof. **Repaired in this follow-up:** the confirmed initialization callback-loss defect now has a bounded buffer, overflow refusal and connection-teardown guard, with seven public-dispatch/controlled-backend regressions in [initialization proof](cc08/initialization/README.md). This does not establish the remaining native coverage or transport-ordering requirements. |
| CC-09 / 302–316 | Queue/capture phase counters, 24-case synthetic native-pixel matrix, producer-only real-clock tests and real no-model storage smoke (`cc09/README.md`, `cc09/native-measurement-boundary.md`) | Native event-to-storage correlation and polling-inclusive latency; representative load distributions; native retained-cache/queue byte sizing; full transport/serialization attribution; real cancellation acknowledgement and stop-further-emissions contract. Client close and process cleanup are not cancellation acknowledgement. Baselines precede budgets; no new SLA or parallel native effects. Preserve authenticated/protected remote access, socket permissions, disposable desktops, and grants' pixel/physical-input limitations. |

## Separate Mousepad acceptance and blockers

The 32-step model-assisted visual batch and independent public-pointer reopening are separate bounded proofs. Original 24-step unpooled model acceptance remains open. Semantic Open's rejected peer-routing experiment is retained on the Mousepad branch in `semantic-open/`; all three attempts failed later readback, all source bytes were unchanged, and the patch was reverted. A local native bridge build stopped at missing `libxml-2.0` development metadata. No system library replacement was made. Do not retry that route indefinitely or relabel it GREEN.

Section 13's packaged specialist, consumer adapters and skills/learning are explicitly future feature work, not CC-10 or additional numbered remediation. Sections 14's integration, fresh regression/gate/mutation receipts, independent review and honest public evidence remain shipping gates. This audit does not substitute for those gates.
